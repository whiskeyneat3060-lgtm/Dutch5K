// GENERATED FILE — replace this template by running:
//
//     dart pub global activate flutterfire_cli
//     flutterfire configure --project=<your-firebase-project-id>
//
// That writes lib/firebase_options.dart plus android/app/google-services.json
// and ios/Runner/GoogleService-Info.plist. Those three files are gitignored
// because they are per-project configuration, not source.
//
// This template exists so the project analyses and builds before Firebase has
// been configured; it throws at runtime rather than failing silently.
import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  const DefaultFirebaseOptions._();

  static FirebaseOptions get currentPlatform => throw UnsupportedError(
        'Firebase is not configured. Run `flutterfire configure` to generate '
        'lib/firebase_options.dart. See README.md, section "Firebase setup".',
      );
}
